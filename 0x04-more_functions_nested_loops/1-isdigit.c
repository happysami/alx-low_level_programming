#include "main.h"
/**
 * _isdigit  - Chek  that help or not
 *@x: Is for the numbers we de
 * Return: This will 0 and 1
 */
int _isdigit(int x)
{
if (x >= 48 && x <= 57)
{
return (1);
return (0);
}
}
