#include "main.h"
/**
 * _isupper - Chek if a letter is upper
 * @x: The number to be cheked
 * Return: return value 0 or 1
 */
int _isupper(int x)
{
if (x >= 65 && x <= 90)
{
return (1);
}
return (0);
}
